@startuml
start

:CRON задача;

:Запуск скрипта
python script.py
**>> index_update.log**;

:Чтение файлов из
Task6/docs/*.md,*.txt;

if (Файл уже в индексе?) then (да)
  :Пропустить файл;
else (нет)
  :Разбить текст на чанки
  (по 800 символов);
  
  :Создать эмбеддинги
  через BAAI модель;
  
  :Добавить векторы
  в FAISS индекс;
  
  :Сохранить метаданные
  в metadata.json;
endif

:Записать индекс
в файл faiss.index;

:Запись логов в файл
index_update.log;

stop

@enduml